export abstract class Memory {

    static native size(): i32 {
        memory.size 0;
        end;
    }

    static native grow(delta: i32): i32 {
        memory.grow;
        end;
    }

    static native copy(source: i32, target: i32, size: i32): void {
        memory.copy;
        end;
    }
}
