enum SectionType {
    CUSTOM,
    TYPES,
    IMPORTS,
    FUNCTIONS,
    TABLES,
    MEMORY,
    GLOBALS,
    EXPORTS,
    START,
    ELEMENTS,
    CODE,
    DATA,
    DATA_COUNT
}

export default SectionType;
