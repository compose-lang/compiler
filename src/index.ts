import Pipeline from "./compiler/Pipeline";

export default function main(...args: string[]) {
    const verb = args[0];
    switch (verb) {
        case "compile":
            // Pipeline.buil(args);
            break

    }
}
