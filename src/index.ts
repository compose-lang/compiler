import Compiler from "./compiler/Compiler";

export default function main(...args: string[]) {
    const verb = args[0];
    switch (verb) {
        case "compile":
            // Compiler.compile(args);
            break

    }
}
