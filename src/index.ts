import Pipeline from "./compiler/Pipeline.ts";

export default function main(...args: string[]) {
    const verb = args[0];
    switch (verb) {
        case "compile":
            // Pipeline.buil(args);
            break

    }
}
